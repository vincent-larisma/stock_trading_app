<div class="">
  <section class="user-show-form">
  <p class="user-show-tag"> Details </p>
  <hr>
<table class="show-table-attribute">
    <tr>
        <th>Email:</th> 
        <td>&nbsp;<%= @user.email %></td>
    </tr>
    <tr>
        <th>Role:</th> 
        <td><%= @user.role.capitalize %></td>
    </tr>
    <tr>
        <th>Account Status:</th> 
        <td><%= @user.account_status.capitalize %></td>
    </tr>
</table>
    <div class="actions d-flex justify-content-center mt-3 gap-4"> 
        <%= button_to "Edit", edit_admin_user_path(@user.id), method: :get, class: "btn btn-primary", id: "edit-user-submit-btn"  %>
        <%= button_to "Delete", admin_user_path(@user.id), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-primary", id: "edit-user-submit-btn" %></td>
    </div>
</section> 
</div>
